Activate all relevant skills and plugins first.

Task: stage and commit the audit-log prettier-formatting cleanup as a single local commit. Do not broaden scope. No push.

Workspace:
D:\Vscode\hms-login-OFFICIAL

Scope of this commit (2 files only):
- hms-backend/src/audit/audit.service.ts      (prettier formatting on findMyEvents, findEntityTimeline, verifyChainWithSignatures, exportEvents)
- hms-backend/src/audit/audit-event-keys.ts   (prettier formatting of AuditEventKey type line break)

Verify before staging:
1. cd hms-backend && git diff --stat hms-backend/src/audit/audit.service.ts hms-backend/src/audit/audit-event-keys.ts
2. Confirm both diffs contain ONLY formatting changes (no added/removed identifiers, types, decorators, or logic).
3. cd hms-backend && git diff hms-backend/src/audit/audit.service.ts hms-backend/src/audit/audit-event-keys.ts | grep -E "^[+-][^+-]" | grep -vE "^[+-]\s*$" | head
   — confirm no semantic lines (e.g., no new @ decorators, no new function calls).

Then:
4. git add hms-backend/src/audit/audit.service.ts hms-backend/src/audit/audit-event-keys.ts
5. git status — confirm ONLY those 2 files are staged.
6. Re-run gates:
   - cd hms-backend && npm run lint        (must be 0 errors)
   - cd hms-backend && npm test             (must be 77 suites / 1537 tests PASS)
   - cd hms-frontend && npm run typecheck   (must be PASS)
   - cd hms-frontend && npm run lint        (must be PASS)
   - cd hms-frontend && npm run test -- --run   (must be 73 files / 406 tests PASS)
7. Commit with message:
   style(audit): apply prettier formatting to audit service and event keys

   - audit.service.ts: reformat findMyEvents, findEntityTimeline, verifyChainWithSignatures, exportEvents to prettier line-width 100
   - audit-event-keys.ts: wrap AuditEventKey type expression across two lines per prettier

8. Report exact commit hash, staged file list, validation results, and remaining modified tracked file count.

Hard constraints:
- Do not push.
- Do not stage any other file.
- Do not edit code.
- Do not amend, rebase, or rewrite history.
- If any diff is found to contain semantic changes, STOP and report — do not proceed.
